/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This tells Vercel to ignore linting errors so it never blocks your deployment
  eslint: {
    ignoreDuringBuilds: true,
  },
  // This tells Vercel to ignore TypeScript type check mismatches during build phase
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
